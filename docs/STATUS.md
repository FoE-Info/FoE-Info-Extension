# FoE-Info Extension — Live Work & Todos

Shared status board for all agents. **Update this file in the same change that
changes the underlying work** — this is the current-status source of truth, not
a changelog. See `docs/README.md` for the full hub.

> Standing constraint from `docs/COORDINATION.md`: no commit/push without the
> user's explicit approval; investigate-and-report before fixing; one verified
> change at a time.

| F2 Session 2 — Great Buildings info decoupling | Done | Removed all three production `msg/ → ui/` imports from `GreatBuildingsService.js` (466 → 468 L) and dropped the unused `export { renderGbDonationPanel }` re-export. New `src/js/state/GreatBuildingsState.js` (channels `donors`/`info`/`donation`) and `src/js/ui/greatBuildingsRenderBinding.js` (injects `renderGbDonorsCard`/`renderGbInfoPanel`/`renderGbDonationPanel`), registered in the `renderBindings.js` composition root. `showGreatBuldingDonation()` now publishes the prepared payloads in order (donors → info → donation) after `fCheckOutput()`; the donors renderer still fills `Top`/`GBrewards`/`Reward` in place so the donation payload reads the same arrays. Source-coupled close/reopen assertion retargeted to the publish + binding. Tests: `tests/state/great-buildings-state.test.mjs`, `tests/ui/great-buildings-render-binding.test.mjs`, extended `tests/ui/render-bindings.test.mjs`. Verification: `npm run verify` exit 0 — 1,201 tests / 0 fail. Worktree `feat/f2-gb-info-decoupling` (base `afe08b3`). |
| LoW-Tool Fork Exclusion Record (Roadmap D3g) | Done | Added the tracked record `docs/specs/2026-09-12-low-tool-fork-exclusion-record.md`. Documents that LoW-Tool is a **downstream fork of FoE-Info with a private `src/extras/` overlay** (per `LoW-Tool/CLAUDE.md:7`), not the upstream original, and records the overlay as intentionally excluded for hardcoded Discord webhooks, an embedded shared Google Apps Script key, and per-world player-ID allowlists. Also records host-only features (antique dealer, settlements), the BigNumber hybrid lineage (LoW-Tool already used CEIL/HALF_UP; host improved owner-safe-add), and the one safe capability candidate (generic opt-in coordination posting on the existing sanitized `post.js`). Secret values deliberately not reproduced. |
| F2 Session 0 / F7 Composition Root + sideEffects Fix | Done | Created `src/js/ui/renderBindings.js` (single side-effect composition root that requires the five `*RenderBinding.js` modules) and replaced the five bare binding imports in `src/js/index.js` with one barrel import. **Root-cause fix:** `package.json` declared `"sideEffects": ["*.css","*.scss"]`, marking every `.js` file side-effect-free, so webpack tree-shook the binding wiring out of `app.js` — at HEAD the built bundle contained `setupIndexBridge` but no `armyRenderBinding`/`renderArmyPanel`, meaning the reactive `msg → ui` bindings never shipped. Added `src/js/ui/renderBindings.js` and `src/js/ui/*RenderBinding.js` to `sideEffects`; a clean `build:dev` now emits all six modules plus the binding functions. New `tests/ui/render-bindings.test.mjs` (5 tests: subscriber wiring + sideEffects contract guard). Verification: `npm run verify` exit 0 — 1,163 tests / 0 fail. |
| Local Toolchain Setup & Webpack Build Cleanup (working tree) | Done | Pinned the dev toolchain to Node 26.8.2 in `.mise.toml` (`[tools]` + a `setup` task; generated `mise.lock`) and added `.npmrc` (`legacy-peer-deps=true`, required because `typescript-eslint@8` peers `typescript <6.1` while the project runs `typescript@^7`). Consolidated the build onto a single dynamic `webpack.config.js` (deleted the `webpack.dev.js`/`webpack.prod.js` thin delegators) and removed four unused toolchain deps: `postcss-loader` (no PostCSS config), `webpack-bundle-analyzer`, `webpack-extension-manifest-plugin`, and `zip-webpack-plugin` (packaging runs through `scripts/package-extension.js`). Added `acorn`/`acorn-walk` (used by `tests/msg/startup-hot-path-logging.test.mjs`). Dropped the `browser-polyfill.js` CopyPlugin patterns and the HTML `<script src="browser-polyfill.js">` tags — the polyfill is bundled via `ProvidePlugin`/direct imports and the manifest only loads `xhrInterceptor.js`/`contentBridge.js`. Moved the dev filesystem cache into the shared config and removed the invalid `HtmlWebpackPlugin.manifest` option; `.graphifyignore` now excludes all of `tests/` (was `tests/agents/`) to keep the 45 MB RPC fixtures out of the AST. Verification: `npm test` 1,158/1,158, eslint 0 errors, `tsc --noEmit` clean, `build:dev` compiles. |
| Residual Build/Toolchain Bloat Removal (working tree) | Done | Deleted the orphaned `src/chrome/manifest_firefox.json` (empty `name`/`version` template with no `browser_specific_settings`; zero references after the manifest-plugin removal), removed the unused `path` require from `webpack.common.js`, and repointed the stale `webpack.dev.js`/`webpack.prod.js` cases in `tests/agents/hooks.test.mjs` to the live `webpack.config.js`/`webpack.common.js`. Corrected the stale `build/FoE-Info_WEBSTORE/manifest.json` path to `build/FoE-Info-Prod/` in the `package-release` skill. Reverted a false-positive `shell-quote` dependency removal after `rg` skipped the hidden `.agents/` directory — it is required by `.agents/scripts/graphify-guard.mjs` (`createRequire(...)('shell-quote')`). Verification: `npm test` 1,158/1,158, eslint 0 errors, `tsc --noEmit` clean, `build:dev` compiles; `npm run verify` is red only on the untracked F2 plan doc's Prettier check. |
| B5 DOM Decoupling (msg → ui) | Done | Routed residual `src/js/msg/` DOM through `src/js/ui/`: new `renderTreasuryLogPanel.js`, `renderCulturalPanel.js`, `renderBonusPanel.js`, `renderResourcePanel.js`, `renderExpeditionPanel.js`, `renderArmyPanel.js`, `renderRewardsPanel.js`, and `renderGbDonationLegacy.js`. Each service now parses/owns state and delegates markup+binding; `OutpostService`'s `renderCulturalPanel`/`setShowOptions` and `GbDonationService`'s `renderGbDonationPanel`/`handleNewReward` public APIs are preserved via re-exports. Treasury 373→283 L, Outpost 455→321 L, Army 403→275 L. Regression: `tests/ui/render-treasury-log-panel.test.mjs`; source-coupled assertions retargeted. Verification: `npm run verify` exit 0 — 1,087 tests / 0 fail. |
| Structured DevTools↔Panel Bridge (Actionable Item 4) | Done | Added `src/js/protocol/devtoolsBridge.js`: versioned `postMessage` envelopes (`raw-network-entry`/`request-finished`/`panel-ready`/`host-ping`) with a per-show readiness handshake, replacing the global `window.handleRawNetworkEntry` attachment for the `devtools.js` ↔ `index.js` path. `devtools.js` posts structured entries and re-pings on `onShown`; `index.js` installs the panel listener via `installPanelBridge`. `networkListener.js` still owns its own `window.handle*` globals for CDP/back-compat. New `tests/protocol/devtools-bridge.test.mjs` (8 tests). Verification: `npm run verify` exit 0 — 1,095 tests / 0 fail. |
| Copyright Header Removal & File-Purpose Headers | Done | Removed the 13-line `Copyright (C) 2022 FoE-Info` comment block from all 20 source files (copyright retained in the repo's dedicated copyright file) and added one-line `/** … */` purpose headers to the 31 `src/js` files that lacked a leading description. Verification: `npm run verify` exit 0 — all tests / prettier/lint/typecheck green. |
| Reactive QuantumState (Actionable Item 2, slice 1) | Done | Added `src/js/state/QuantumState.js` (publish/subscribe `notify(changed)` store mirroring `BlueGalaxyState`). `GuildRaidsService` now publishes parsed member-activity/leaderboard data instead of importing `renderQuantumPanels`; `renderQuantumPanels` self-subscribes and repaints only the changed card, loaded via a side-effect import in `index.js`. Tests: `tests/state/quantum-state.test.mjs`, reactive assertions in the guild-raids and quantum-panel suites. Verification: `npm run verify` exit 0 — 1,103 tests / 0 fail. |
| Reactive StartupRenderState (Actionable Item 2, slice 2) | Done | Added `src/js/state/StartupRenderState.js` (channeled `city-stats`/`building-collection` store). `StartupService` no longer imports `renderLiveCityStats`/`renderBuildingCollectionTimes`; it publishes context through the store. New `src/js/ui/startupRenderBinding.js` subscribes the two UI renderers and is loaded via `index.js`. Tests: `tests/state/startup-render-state.test.mjs`, `tests/ui/startup-render-binding.test.mjs`. Verification: `npm run verify` exit 0 — 1,114 tests / 0 fail. |
| Reactive-store graph audit + F6 debug-logging fix | Done | Refreshed the FoE-Info AST (3,512 nodes / 5,702 edges / 294 communities) and audited Actionable Item 2 against it. Confirmed the stores landed with inverted edges (`service → state ← ui`) and `calc/` purity intact; catalogued residual coupling: 29 production `msg/ → ui/` import edges across 14 services and 3 `ui/ → msg/` inversions. Fixed **F6**: `notify()` silently swallowed subscriber errors — both stores now accept an injectable scoped logger and emit `logger.error('Reactive subscriber failed', …)`. Verification: `npm run verify` exit 0 — 1,116 tests / 0 fail. Findings: `graphify-out/foe-info/findings/2026-09-12-reactive-store-migration-audit.md`. |
| Debug Stub Live Sync, Panel Targeting & Context Visibility | Done | Reworked debug mode in `cardVisibility.js`/`.ts`: stubs are now kept live by a `MutationObserver` (refresh on mount/re-render), injected inside each panel's primary card (`.alert`/`foe-card`/`.card`) so they read as attached, and only for the visible topmost non-empty panel (wrappers/empty shells skipped). Targets corrected: Lists (`friends`) split into `friendsText`/`guildText`/`hoodText`; GE uses `geChampionshipCard`/`geContributionCard`; QI uses `quantumContributions`/`quantumLeaderboard`. Visibility fixes: added `friends` to `OWN_CITY` (gated by `showFriends`); added `PANEL_PARENT` mappings `geContributionSection`/`geInternationalSection` → `donationDIV2` so the hidden GE wrapper is revealed; `applyUnconstrainedVisibility` reveals the `goodsInventory` wrapper; `renderGoodsPanel` reveals its wrapper on render. Verification: `npm run verify` exit 0 — 1,095 → 1,082 tests / 0 fail after removals; live-verified in OWN_CITY (Lists + per-checker stubs, goods, rewards). |
| Reward Raw-Id Display Mapping | Done | `fRewardShortName` now maps raw engine ids via `fResourceShortName` aliases (`strategy_points` → Forge Points, `medals` → Medals, `clan_power` → Guild Power) and title-cases leftover snake ids (`rogue` → Rogue), fixing `quest`/`cityProductionCity` rewards that carry `name` without `type`. Tests in `tests/utils/formatters.test.mjs`. |
| GB Level-Closing Badge Removal | Done | Removed the unrequested `level-closing-badge` alert (and its now-unused `remainingTotal`/`p1Base`/`arcBonus`/`p1Reward` locals) from `renderGbDonationPanel.js`; dropped the two UI tests. The pure `calculateLevelClosingProfit` calculator is retained. |
| Town Hall Beta Debug Panel Removal | Done | Deleted `src/js/ui/betaDebugPanel.js` and its two suites; removed wiring from `StartupService.js`, `BonusService.js` (`getBonuses` + `cityRoutes.js`/`indexBridgeSetup.js` registrations), `CityMapEntityProcessor.js`, and `CityEntityHarvestCalculator.js` (including the calc-purity-violating `appendBetaText` calls). `BonusService.getBonuses` triaged as allowed-unhandled in `scripts/rpc-contract.config.json`. Verification: `npm run verify` exit 0 — 1,082 tests / 0 fail, RPC contract 0 unhandled. |
| Galaxy Context Gating | Done | `renderGalaxyPanel` now hides `#galaxy` when the active context does not allow `galaxy` (GBG/GE/QI/SETTLEMENT/OTHER_PLAYER), so a late render can no longer override the context engine. Regression test in `tests/ui/render-galaxy-panel.test.mjs`. |
| Browser Control Script Removal (working tree) | Done | Removed the tracked CDP browser-control script from `scripts/` and dropped its reference from `.agents/rules/browser-environment-hygiene.md`; the external `foe-browser` launcher stays untracked. Pending: purge the script from prior commits (requires history rewrite + force-push). |
| Build-Tier Matrix & Dev Forced-State Panels | Done | Split prod/beta/dev into distinct tiers in `webpack.config.js`: new `DEBUG_BUILD` (beta/dev) and `FORCE_FIXTURES` (dev) DefinePlugin flags; beta `pure_funcs: []` keeps `console.debug/info` while prod strips `debug/info/log` and preserves `warn/error`; `logger.js` gains a compile-time `DEBUG_BUILD` gate and defaults debug on for beta/dev only (never Node/tests). Removed the ungated `rpcLogger` `console.debug` else-branch and routed `ConversationService`'s raw `console.log` through `logger.error`. Dev target adds a filesystem cache, a dev-only `app` entry (`src/js/dev/forcedStateBootstrap.js`), and copies curated `tests/fixtures/forced/` + RPC captures into `build/FoE-Info-DEV/fixtures/` (absent from beta/prod). Seeders replay captured `CityMapService.getEntities` into `blueGalaxyState` and a saved reward template through the real `showReward` pipeline, with a dev-only re-seed button for post-startup resets. New i18n keys `dev_seed`/`dev_seed_done`/`dev_seed_failed` × 7. Verification: `npm run verify` exit 0 — 1,045 tests / 0 fail; `build:beta` keeps 28 `console.debug`, `build:prod` 0 and no `fixtures/` in either. |
| Parallel Open-Items Execution (OpenCode subagents) | Done | Four disjoint parallel tasks: (1) dedup extraction — `MessageDispatcher.js` 607 -> 583 L, new `dedupCache.js` (73 L) + 8-test suite; (2) calc purity — `gbNaming.js` dead `globalThis` fallbacks removed, new `tests/calc/calc-purity.test.mjs` guard; (3) TS mirrors — `CityStatsCalculator.ts` (393 L) + `MetadataStore.ts` (595 L), `tsc --noEmit` clean; (4) dispatcher resilience tests — `tests/protocol/dispatcher-resilience.test.mjs` (18 tests). Verification: `npm run verify` exit 0 — 1,078 tests / 0 fail, prettier/lint/typecheck/RPC-contract/i18n green, dev bundle compiles. |
| Dev Forced-State Seeder Debug Gating | Done | Replaced the dev-build-only gate on the forced-state seeder with a debug-mode gate. `src/js/dev/forcedStateBootstrap.js` now exposes `createForcedStateController` and mounts/seeds only while `isDebugEnabled()`, subscribing to `onDebugToggle()` so toggling the header bug icon mounts or tears the control down live; `src/js/dev/devSeedButton.js` gains `unmountDevSeedButton()`. Fixture requires are now lazy inside the `FORCE_FIXTURES` guard so the module imports headlessly. New `tests/dev/dev-seed-gating.test.mjs` (7 tests). Verification: `npm run verify` exit 0 — 1,085 tests / 0 fail, prettier/lint/typecheck/RPC-contract/i18n green, dev bundle compiles. |
| Unhandled RPC Log Filtering & Reversible Scope Toggle | Done | Added `src/js/protocol/rpcScope.js` (`IGNORED_RPC_CLASSES` = 21 out-of-scope services) wired into `rpcLogger.js` + `MessageDispatcher.js`, so unhandled responses from those classes are hidden from the `[FoE-Info:RPC]` console groups and `window.foeRpcLog`; in-domain unhandled stay visible. Reversible via `window.foeShowIgnoredRpc(true\|false)` (persisted under `showIgnoredRpc`). Dropped already-handled `GreatBuildingsService.getAvailablePackageForgePoints` from `allowedUnhandled`. New `tests/protocol/rpc-scope.test.mjs` guards runtime/config parity; documented in `docs/debugging.md` section 4. |
| Panel Checkbox Theming Fix (GBG + Options) | Done | The GBG "show changes only" checkbox was a bare native `<input>` that rendered as a dark UA widget on the light `alert-info` card under `color-scheme: light dark`; converted to Bootstrap `form-check`/`form-check-input` (QI/Invested parity) in `renderBattlegroundsPanel.js`, with an `accent-color`/`color-scheme: light` fallback for remaining native checkboxes in `custom.scss` and `options.scss`. |
| Debug Visible-Panel Data Stubs | Done | Reworked `cardVisibility.js`/`.ts` debug mode to stop force-showing all 15 panels: it now applies active context/options visibility and prepends a `[DEBUG STUB] <id>` block to each visible panel containing a collapsed, escaped dump of the panel's rendered content. Updated `tests/ui/context-view-filtering.test.mjs`. |
| Reward Label Fallbacks (Forge Points + Units) | Done | `fResourceShortName` now falls back to core display aliases (`strategy_points` -> Forge Points, `money` -> Coins, `medals`, `premium` -> Diamonds, `clan_power` -> Guild Power, ...) before returning a raw id; `CityProductionService` unit names fall back to `fTitleCase(unitTypeId)` (`rogue` -> Rogue). New `fTitleCase` + tests in `tests/utils/formatters.test.mjs`. |
| City Overview Layout Redesign (Own + Visited) | Done | Rebuilt `#citystats` / `#visit` to the specified layout: static `City Overview` header (new `city_overview` key × 7 locales) with collapse toggle, plain-text `Copy`, and a `foe-card-divider` rule; player identity moved into the body; Guild → Age → Score order; section headers render as `── DAILY PRODUCTION ────` text rules with no background. Removed the green `badge bg-success` Copy pill and the header info-icon popover. Visited card mirrors the layout with **zero popovers/tooltips** (plain FP/Goods/Guild Goods/Units; shield as plain text). Added `.foe-card-divider`; `.foe-section-header` now uses `::before`/`::after` rules. Updated `city-card-bonuses` and `city-stats-calculator` tests. Verification: `npm run verify` exit 0 — 1,041 tests / 0 fail, i18n 232 keys × 7, prettier/lint/typecheck/build green. **Follow-up:** collapsed header now shows the player name (expanded keeps `City Overview`); `Copy` gained a circular `currentColor` outline; all scrollbars unified to an 8px WebKit bar with the standard `scrollbar-width`/`scrollbar-color` scoped behind `@supports not selector(::-webkit-scrollbar)` so Chromium no longer ignores the custom width. Verification: `npm run verify` exit 0 — 1,043 tests / 0 fail. **Follow-up 2:** dropped `text-dark` from the own card header/name so they inherit the card theme; the collapsed-title swap is now live via `.foe-original-card:has(> .collapse:not(.show))` toggling `foe-title-expanded`/`foe-title-collapsed` + a header info icon; `formatStatsText` now copies the body only, strips icon ligatures, and formats section headers as `Daily Production:` / `Combat Boosts:` with blank-line separators. Verification: `npm run verify` exit 0 — 1,043 tests / 0 fail. |
| FoE Expert Third-Pass Audit — Graph + HAR Edge-to-Edge | Done | Third pass re-verified all 13 `foe-*` experts edge-to-edge via 5 parallel domain audits against the 104-RPC extract corpus, 2,875 metadata entities, and the live metadata graph (5,499 nodes / 46,623 edges / 383 communities); every hard finding spot-verified against raw JSON/source. **Supersedes three first-pass claims:** QI action-point capacity/regen IS captured (`autoRefill` 3600 s / 5000 / 224000; premium 15), `getConstruction` request is `[entityId, playerId]` (the 3-tuple is `getConstructionRanking`; `contributeForgePoints` is `[entityId, playerId, level, fpAmount, boolean]`), allies level with Heroic Scrolls + Valor Tokens (not "experience"). **Other hard fixes:** `preferredUnitIds` present but empty; Neo Winners' Plaza IS a real QI building; GE relics are 3 rarities (not 4); `connected` values `{1,2,unset}` not `0/1`; SelectionKit/UpgradeKit payload identifiers; PvP attempt `autoRefill` 5760 s / 5 + 50-diamond price captured; wildlife uses `wildlife_pop_moves` not chests; Halloween is wheel-of-fortune; `ServerRequest` carries `requestData`, `ServerResponse` carries `responseData`; named real QI `GuildRaids*` classes and `GuildBattlegroundSignalsService`; softened unverified GE 5 / turn / lock-duration / season-length / streak claims. Verification: `node --test tests/agents/agent-config.test.mjs` 13/13; `npm run verify` exit 0 — 1,041 tests / 0 fail, prettier/lint/typecheck/i18n/RPC/dev-build green. Canonical counts unchanged (36/17/53). Follow-up: ported `calculateLevelClosingProfit` into the tracked `.ts` mirror with a typed `GreatBuildingLevelClosingProfit` return interface (`tsc --noEmit` clean). |
| FoE Expert Payload Audit — Edge-to-Edge (metadata graph + captures) | Done | Second, exhaustive audit of all 13 `foe-*` experts against the metadata graph (5,499 nodes / 46,623 edges / 383 communities) and the 44-HAR extract corpus (9,368 game requests, 103 RPC payloads, 27 bundles, 13 visited cities) via 6 parallel domain audits; every hard finding spot-verified against raw JSON/source. **GB:** `getConstruction` response is `rankings` only (level is a request arg), `getOtherPlayerOverview` rows use `player.player_id` (no `other_player.id`), corrected owner-safe-add/spot-lock formulas, Arc scales strategy-points only (not blueprints/medals), no `globals.playerId`. **Sniping:** `OtherPlayerService.getOtherPlayerOverview` is registered. **GBG:** province IDs `A1`/`B1`… (not `A1A`–`D4X`), real `availableBuildings`, GBG odds use native math. **QI:** AP capacity/regen absent from captures, boss = `fightType`, real reward buildings. **Settlements:** 7 not 6 (Pirates), Aztec goods `vegetables`/`headdress`/`maize`/`stone_figures`. **Combat:** `unitClass` enum, Himeji=Spoils/battles vs Space Carrier=Diplomatic/negotiations, 1,005 boosts. **Allies:** `spartan_soldier` IDs, 5 rarities, `military`/`science` rooms, experience-not-fragments. **City:** 72×72 grid, `width`/`length`, numeric `connected`. **Game-data:** single `Startup` object, `building_entity_lookup` identifiers, incidents via `HiddenRewardService`. **Antiques:** `outputModifier` 1.0/1.25/1.5 = +0/25/50%. **Event/PvP:** unverified method/mechanic claims softened. Verification: `node --test tests/agents/agent-config.test.mjs` 13/13, `npm run verify` exit 0. |
| FoE Expert Ground-Truth Correction (metadata-store Extracts) | Done | Corrected factual drift in the FoE domain subagents against the authoritative extract corpus (`../metadata-store/extracts/`, 103 RPC payloads) and runtime source. **Rounding:** fixed the false `ROUND_CEIL`-for-Arc-rewards claims in `foe-great-buildings-expert` and `foe-sniping-expert` to the half-up/ceil hybrid (`GreatBuildingCalculator.js` rewards half-up, locks/owner-adds ceiling), matching the `bignumber-precision` rule and `foe-game-data-expert` (already correct). **RPC names:** replaced the nonexistent `CityProductionService.fGetEntityList` with `CityMapService.getEntities` and corrected `GbDonationService.getContributions` → `GreatBuildingsService.getContributions` (registered in `buildingRoutes.js`, uncaptured). Audited all `Class.Method` refs across 13 `foe-*` experts; remaining non-corpus refs are deliberate negatives (`GreatBuildingsService.getOverview`, `OtherPlayerService.getOtherPlayerOverview`) or locally-registered/uncaptured (`ClanService.getTreasury`). Verification: `node --test tests/agents/agent-config.test.mjs` 13/13 and `npm run verify` exit 0 — 1,041 tests / 0 fail, dev build compiles. |
| Invariant Gate Remediation (Gates 1/4/6/7/8) | Done | Remediated the multi-subagent code-review gate failures. **Gate 1 (monolith):** extracted `resolvePlayerScore` to `src/js/state/playerScoreResolver.js` (injected deps + logger) and boost wiring to `StartupBoostCoordinator.subscribeBoostUpdates`; `StartupService.js` 537 → 508 lines, now a thin delegate. **Gate 4 (file cap):** extracted `src/js/ui/components/debugToggle.js`; `containerBinding.js` 604 → 571; also fixed the keyboard double-fire (logo sets `data-foe-native-keys`, `AddElement.js` global handler opts out). **Gate 6 (i18n):** broadened `translateContainer` to independently handle `data-i18n-title`/`-aria-label`/`-placeholder`; repaired corrupt `gr.json` (475 U+FFFD → 0); translated all series-added keys; fully localized `options.html` (78 bindings, +72 `opt_*` keys, `initOptionsI18n` in `options.js`); bound GBG/GE/GB table headers, debug/settings labels, `Unknown Building`, `Player Information`, `None`, and AddElement aria-labels. Parity now 231 keys × 7 locales. **Gates 7/8:** added `logger.warn` to silent catches in `BoostService.js`/`OtherPlayerService.js`; resolver logs resolution/failures. Tests: new `tests/ui/debug-toggle.test.mjs` and real `resolvePlayerScore` cases replacing the tautological fallback test. Verification: `npm run verify` exit 0 — 1,041 tests / 0 fail, i18n 231×7, prettier/lint/typecheck clean, dev build compiles. Committed with this changeset. |
| Modern-Web Guidance Expert & Skill Overlay | Done | Captured the enforced modern-web conventions (a11y live regions, `color-scheme`/`light-dark()`, options form validation, `scheduler.js` deferral, `resolveDate()` timestamps, MV3 CSP) in a single-source overlay `.agents/skills/modern-web-guidance/references/project-conventions.md`, linked from that skill's `SKILL.md`, and cross-linked a "Modern Web Guidance (Project Overlay)" section into 13 web/UI/QA specialists (`ui-design-system-architect`, `accessibility-specialist`, `javascript-expert`, `typescript-expert`, `webpack-expert`, `performance-memory-profiler`, `extension-security-auditor`, `chrome-extension-architect`, `code-reviewer`, `cdp-test-engineer`, `extension-release-engineer`, `monolith-refactoring-specialist`, `codebase-modernization-architect`) and 14 skills (`add-feature-panel`, `ui-ux-pro-max`, `a11y-debugging`, `migrate-jquery-to-native`, `frontend-security-coder`, `debug-optimize-lcp`, `fixing-motion-performance`, `audit-memory-leaks`, `browser-testing`, `chrome-devtools`, `codebase-audit-pre-push`, `test-guard`, `add-rpc-service`, `cross-platform-contract-propagation-audit`). Fixed a pre-existing unclosed code fence in `add-rpc-service/SKILL.md` exposed by the append. Canonical counts unchanged (36/17/53). Verified `node --test tests/agents/agent-config.test.mjs` 13/13 and `npm run verify` exit 0 (1,033 tests / 98 suites, prettier clean, 0 eslint errors, i18n 100%, dev build compiles). |
| Modern-Web-Guidance Audit & Tier 1/2 Remediation | Done | Audited the runtime against the local 141-guide Chrome-team `modern-web-guidance` library with 5 parallel specialist subagents (performance, ui-behaviors/atoms/components, css, visual-design, js, accessibility, html, forms, built-in-ai, webmcp, security, privacy). Implemented Tier 1/2 via 4 parallel file-partitioned subagents: **performance** (new `src/js/utils/scheduler.js` yielding, low-priority enrichment fetches, long-lived `ResizeObserver`, cached `Intl.NumberFormat`, `innerHTML +=` removal), **JS UI a11y** (16 informational `role="alert"` -> `role="status" aria-live="polite"`, shared `#foeCopyStatus` copy live region, Space-on-keyup activation, `aria-expanded` sync, single focusable collapse control, table `<caption>`/`scope`, popover Escape + ARIA, debug-toggle button semantics), **CSS/HTML/forms** (`color-scheme` meta on all pages, standard `scrollbar-color` + `prefers-contrast`, `prefers-reduced-motion`, `<form id="optionsForm">` with `name`/constraints/`:user-invalid`, CSP `base-uri 'none'`), and **date correctness** (removed `dayjs`, `resolveDate()` seconds/ms normalization, raw `toLocaleTimeString` -> `formatInTimeZone`). Extra: fixed the same units bug in `incidentsPanel.js` (~1000x wrong incident countdown) and added `copied`/`copy_failed` across all 7 locales. Deferred Tier 3 (native popover/anchor-positioning migration, `light-dark()`, Built-in AI/WebMCP). Verification: `npm run verify` exit 0 — 1,033 tests / 98 suites, 0 eslint errors, typecheck clean, RPC contract 0 unhandled, i18n 100% (149 keys x 7 locales), dev build compiles. 63 files modified + 2 new. |
| opencode Agent/Skill/Rule Handoff Parity (OpenCode) | Done | Audited dual-harness parity for handing work between Antigravity and opencode: 36/36 exact-name agent shims in `.opencode/agents/`, 53 canonical skills auto-discovered from `.agents/skills/`, 17 rules injected via the `opencode.json` instructions glob, and shared `STATUS`/`HANDOFF`/`plans`/`specs`. Added opencode-only `.opencode/skills/writing-opencode-plugins/SKILL.md` (counterpart to the Antigravity-only `writing-hooks` runbook: `.opencode/plugins/*.mjs`, hook surface, and the no-`PreInvocation`/no-`Stop`/no-`force_ask` limits) and documented it plus the existing `.opencode/instructions/antigravity-tool-translation.md` token map in `docs/OPENCODE.md`. Canonical counts unchanged (36/17/53). Verified `npm run check` clean and `node --test tests/agents/agent-config.test.mjs` (13/13). |
| City Info Panel Redesign (Daily Production & Combat Boosts) | Done | Redesigned `#citystats` (own city) and `#visit` (visited city) with clear visual hierarchy matching user specs: clean header (`[-] [WORLD] PlayerName (i) [Copy]`), top info (`Guild`, `Score`, `Age`, `Arc Bonus`, `Chateau Frontenac Bonus`, `Crit Strike` for AO & CC only), section headers (`── DAILY PRODUCTION ──` and `── COMBAT BOOSTS ──`), inline coin/supply boosts on totals line, units breakdown popover, and symmetrical visited player layout. Merged in `1287743`. |
| Player Score Resolution & Social List Ingestion (Forge-Hammer Parity) | Done | Fixed `Score: 0` bug. Fixed CJS compatibility shim in `src/js/fn/storage.js`; fixed `MyInfo` variable overwrite in `OtherPlayerService.js`; registered `getNeighborList` (American spelling) in `socialRoutes.js`; ingested `payload.members` and `payload.clan.members` from clan services; added synchronous `storage.getSync('playerScore')` and async `chrome.storage.local` fallback in `StartupService.js`; prioritized positive `MyInfo.score` in `renderLiveCityStats.js`. Verified via 1,027 unit tests and 5-stage gate (`npm run verify`). Merged in `d41a64d`. |
| City Overview Permanence, Boosts Wiring & Accuracies (Track 1) | Done | Permanently permit `#citystats` across all contexts in `cardVisibility.ts`/`.js`; wire `BoostService.getAllBoosts` (1,005 server boosts) to `StartupService.boostServiceAllBoosts`; hydrate player score from `OtherPlayerService.getSocialList` (`6,249,698,209`) and persist to `worldStorage`; fix reward ID regex in `CityMapEntityProcessor.js` (daily units); seed historical closed event quests on startup (`QuestService.js`); decouple goods inventory trigger from Market-only lock. |
| UI Panels Sizing, Guild Redesign & Options Reorganization (Track 2) | Done | QI Contributions and GBG Battlegrounds ~20-player bounded scrollable height (off) vs full auto height (on); QI Leaderboard default top 10 guilds with scrollbar; Guild Overview dual header state (collapsed `[+] Guild: <Name>` vs expanded `[-] Guild Overview` with subtitle) + flex layout + `.table-responsive`; clean up City Info card in `options.html` and add `showDailyCoins`, `showDailySupplies`, `showCoinBoost`, `showSupplyBoost` toggles across `options.js`, `showOptions.js`, `ownCityCard.js`, `visitedCityCard.js`, and `src/i18n/`. Verified via 5-stage gate (`npm run verify`, 1,005/1,005 tests). |
| Universal World Defaults & QI Panel Options Toggles | Done | Aligned default settings across all worlds to user specification (Goods Inventory enabled by default, Language defaulting to "Use Game Language" (`game`), QI Member Activity and Leaderboard toggles added, `mergeWithWorldDefaults` hydration in `worldStorage.js`). All 5-stage verification checks passing (`npm run verify`, 1,002/1,002 tests). |
| Orchestrator Thinning (`src/js/index.js` Decomposition) | Done | Decomposed `src/js/index.js` from 566 to 159 lines (-407 lines, 72% reduction); extracted `rpcLogger.js` (65L), `indexBridgeSetup.js` (136L), and `indexEntityDefs.js` (70L); added `bootstrapExtensionUi` to `indexUiBindings.js`. Merged in `b80cc58`. |
| Guild Overview Panel Restoration (OpenCode Session 2) | Done | Fixed `#guildOverview` parent container visibility and wrapper unhiding in `cardVisibility.js` / `cardVisibility.ts` and `renderGuildPanel.js`; passed `renderGuildPanel` explicitly in `index.js` bridge. Merged in `ca256a4`. |
| Number Font Normalization to Standard Sans-Serif (OpenCode Session 1) | Done | Purged `font-monospace` and oversized tabular formatting on numeric cells in `renderQuantumPanels.js`, `OutpostService.js`, and `TreasuryService.js` to match standard sans-serif system font (orange style). Merged in `8cfad22`. |
| GB Donation Self-Leveling Warning Removal | Done | Completely purged misleading "Warning: Open donation places available. Self-leveling will waste donor FP." from `src/js/ui/renderGbDonationPanel.js`, `tests/ui/render-gb-donation-panel.test.mjs`, and specs. Merged in `5720a50`. |
| GB Package Balance & Level-Closing Profit (Antigravity Track 4) | Done | Added pure `calculateLevelClosingProfit` to `GreatBuildingCalculator.js`; registered `GreatBuildingsService.getAvailablePackageForgePoints` in `buildingRoutes.js` and `GreatBuildingsService.js`; wired FP packages balance badge and level-closing profit alert (`level-closing-badge`) in `renderGbDonationPanel.js`. Merged in `07a478b`. |
| Rewards Routing & Reward Categories Centralization (OpenCode Track 2) | Done | Extracted `src/js/ui/rewardCategories.js` (160L), decoupled `CityProductionService.js` and `QuestService.js` from category logic, simplified `RewardRenderer.js` (281 -> 194 lines), and added `tests/ui/reward-routing.test.mjs`. Merged in `c4f9e98`. |
| TypeScript Mirrors Reconciliation & Typecheck Verify Gate (OpenCode Track 3) | Done | Reconciled `BlueGalaxyCalculator.ts` and `GreatBuildingCalculator.ts` with runtime JS; wired `npm run typecheck` into `package.json` verify pipeline and updated package test suite. Merged in `72561d6`. |
| RPC Contract Gate Enforcement & Duplicate RPC Elimination (OpenCode Track 1) | Done | Eliminated all 14 duplicate InnoGames RPC registrations across `registerServices.js`, `cityRoutes.js`, and `GuildRaidsService.js`. Triaged `scripts/rpc-contract.config.json` and added `rpc:contract:check` to `verify` pipeline. Merged in `86ac5d2`. |
| Goods Panel Extraction & Ephemeral Panel Dismissal Lifecycle (Workstream 2) | Done | Shipped in `b217744`: extracted `src/js/ui/renderGoodsPanel.js` (`createLogger('GoodsPanel')`); thinned `src/js/msg/ResourceService.js` 580 -> 354 lines with `isGoodsPanelUnlocked()`/`unlockGoodsPanel()`/`lockGoodsPanel()` (lines 155/159/166) and `.btn-close` `onDismiss: lockGoodsPanel` (line 204); unlock gated to market/inventory triggers. Regression: `tests/msg/resource-market-trigger.test.mjs`. A11y follow-up in `39a5bf7` (live-region announce + control semantics). |
| Roadmap & Status Doc Reconciliation (OpenCode) | Done | Synced `docs/STATUS.md` + `docs/plans/2026-09-12-targeted-surgical-roadmap.md` to verified reality. Marked Workstream 2 (Goods lifecycle) done; checked roadmap A1/A2/A3/B2/B3/B4/C1/C2/D1g; left open B1 (`gbNaming.js` `globalThis`), B5 (residual `innerHTML` in `msg/`), C3 (BG/sniping UX), D2g (dispatcher resilience tests), D3g (LoW-Tool exclusion record) and STATUS Actionable Items 1-4. Recommended next slice: `MessageDispatcher.js` (607 L) `_dedupCache`/`isDuplicatePayload` extraction. |
| Zero Autonomous Browser Control Invariant Hardening | Done | Promoted `.agents/rules/browser-environment-hygiene.md` to `always_on`; updated `.agents/scripts/safety-gate.mjs` with strict mechanical blocks against `foe-browser` and chrome kill commands; added automated hook test coverage (`tests/agents/hooks.test.mjs`). Merged in `6ceb53a`. |
| GB Donation Panel Safe-Spot Selection Fix (Forge-Hammer parity) | Done | Fixed own-GB donation panel targeting already-locked spots. Root cause: `src/js/ui/renderGbDonationPanel.js` selected the first place where `spotLock <= remaining`, which reduces to `occupantInvested <= remaining`, so the boundary case `occupant == remaining` (locked, because passing needs `occupant + 1 > remaining`) was flagged as needing owner FP and safe places were never skipped. Added pure `isPlacePassable(remaining, occupant)` (`occupant < remaining`) to `src/js/calc/GreatBuildingCalculator.js` and used it for the selection predicate. Reproduced against `docs/har/network payload of the GB bug workflow.har` `getConstruction` entities 28479 (Cosmic Catalyst), 19 (Statue of Zeus), 34862 (Blue Galaxy): Cosmic unchanged (3rd, Lock 494, no add); Zeus now targets 3rd (was 2nd, Add 148) -> Add 548; Blue Galaxy now targets 4th (was 3rd, Add 62) -> P3/bakiron recognized safe, Add 316. Added HAR ground-truth + boundary tests in `tests/calc/great-building-calculator.test.mjs`. Verification: `npm run verify` exit 0, 953/953 tests, prettier clean, eslint 0 errors. |
| Context-Driven Panel Visibility Engine (Workstream 1) | Done | Declarative 6-context visibility matrix (`OWN_CITY`, `GBG`, `GE`, `QI`, `SETTLEMENT`, `OTHER_PLAYER`) in `cardVisibility.js` / `cardVisibility.ts`, non-destructive delegation in `panelDispatcher.js`, route transitions in `combatRoutes.js`, `quantumRoutes.js`, `cityRoutes.js`, `socialRoutes.js`, and `donation2` addition to `OTHER_PLAYER`. All 941 tests passing, merged in `b9b41e7`. |
| Great Buildings (GB) Panel Rendering Regression & Own GB Workflow Ingestion | Done | Diagnosed and resolved live regression where GB panels were empty/hidden. Root Cause 1: `fCheckOutput()` in `GreatBuildingsService.js` threw fatal DOM `NotFoundError` calling `contentEl.insertBefore(gbInfoDIV, greatbuilding)` because `greatbuilding` was nested inside `#gbContributors` rather than being a direct child of `contentEl`. Root Cause 2: `webpack.config.js` output casing (`build/FoE-Info-Dev`) diverged from Chrome preferences and `foe-browser` (`build/FoE-Info-DEV`), causing Chrome to execute frozen stale bundles. Root Cause 3: `cardVisibility.js` / `cardVisibility.ts` erroneously toggled `donation2DIV` (GB donation) instead of `donationDIV2` (GE Championship). Verified live via CDP on port 9222: `#donation2`, `#gbInfo`, and `#greatbuilding` render completely with 100% data fidelity. Reviewed own GB securing workflow from extracted live HARs (`open own GB list.har` and `leveled zeus and sw that was full...har`). Verification gate: 941/941 tests passing, `npm run verify` green. Merged in `b08402b`. |
| Quantum Incursions (QI) UX: Contributions & Changes Diffs + Championship Leaderboard | Done | Implemented live QI player activity overview tracking (`GuildRaidsService.getMemberActivityOverview`), diff calculation against stored `world:<id>.qiPerformance`, `+diff` badge highlights in red, "show changes only" filtering, last saved timestamp, and table copy button (`renderQuantumContributionsCard`). Implemented clean 3-column QI Championship Guild Leaderboard (`Guild | Rank | Total Points`) matching the user screenshot (`renderQuantumLeaderboardCard`). Wired `quantumRoutes.js` to `legacyBridge.js`, bound `#quantumContributions` & `#quantumLeaderboard` containers in `containerBinding.js` and `cardVisibility.js`, added collapse toggles in `collapse.js`, 6 new i18n keys synchronized across all 7 language dictionaries, and added unit/UI test suites (`tests/msg/guild-raids-service.test.mjs`, `tests/ui/render-quantum-panels.test.mjs`). Verification: 922/922 tests green. |
| Modernization Phase B DOM Decoupling, Trade Economy & Formula Parity Pinning | Done | Decoupled direct `innerHTML` mutations from protocol/msg handlers (`networkListener.js`, `StartupRenderOrchestrator.js`, `StartupService.js`, `GuildBattlegroundService.js`) into dedicated UI helpers (`gameVersionStatus.js`, `startupMetadataLoading.js`). Implemented `TradeService.js` parsing live marketplace trades and fair trade ratios against real 7.8MB fixture. Added BigNumber formula characterization parity suite (`tests/math/formula-parity-pinning.test.mjs`) pinning half-up Arc rewards, ceiling spot locking, and owner safe adds. |
| Comprehensive Live HAR Ingestion, Multi-Domain Verification & QI (OpenCode) | Done | Streamed 43 live `.har` captures (2.3 GB) via `scripts/ingest-hars-to-metadata.mjs` (`npm run metadata:extract-hars`) into isolated `../metadata-store/extracts/`: 8,306 entries, 9,339 game RPC responses, 103 unique RPCs, 27 domain bundles, 13 visited cities. Baseline `entities/` + `rpc/` mtimes untouched and 0 `.har` tracked. Non-destructive fixtures mirrored to `tests/fixtures/visits/visit-*.json` and `tests/fixtures/rpc/har/`. Phase 2 ground truth: GBG construction/destruction/rushed camps + focus/stop signals (`tests/msg/har-gbg-ground-truth.test.mjs`), Treasury 10-page donation pagination (`tests/msg/har-treasury-pagination.test.mjs`; fixed `TreasuryService` last-page overwrite plus `player_id`/`createdAt` parsing), and 13 visited cities (`tests/fn/har-visited-cities.test.mjs`), plus Great Buildings leveling/donation, hood sniping, and level-closing rewards (`tests/msg/har-great-buildings-ground-truth.test.mjs`). QI protocol contracts + Slice 1-3 module layout in [`docs/plans/2026-09-12-quantum-incursions-architecture.md`](plans/2026-09-12-quantum-incursions-architecture.md). Extracts are indexed into the metadata graph as first-class network payloads (`NetworkRPCPayload`/`PayloadBundle`/`PlayerCitySnapshot`; 5,499 nodes / 46,623 edges) and DeepSeek-relabeled (385 communities); payload-driven panel roadmap in [`docs/specs/2026-09-12-captured-payload-panel-improvements.md`](specs/2026-09-12-captured-payload-panel-improvements.md). |
| Curated Git History Reconstruction (from `8c681d1` to `HEAD`) | Done | Reconstructed clean 14-commit milestone progression from v1 baseline (`8c681d1`) to `HEAD`. All commits and release tags (`v0.0.833`, `v0.0.834`) SSH signed with ED25519 key, 0 code diff against backup release trees, verified with all 849 tests passing. Force-pushed cleanly to `origin/development`. |
| OpenCode Heavy Lifting Implementation (Phase A/B/C) | Done | Completed all 3 sequential tracks: Track 1 (P0 D1 resource lookup + D2 duplicate RPCs + D4/D5 cleanup), Track 2 (P1 `CityMapEntityProcessor.js` 657L -> 254L via `CityEntityHarvestCalculator.js` + `legacyBridge.js` 831L -> 60L via 4 route tables under `protocol/routes/` + D3 calc purity), Track 3 (P1/P2 Blue Galaxy economic ranking + Date Engine Intl tokens). All 849 tests passing, 5-stage verify green. See [`docs/plans/2026-09-12-opencode-heavy-lifting-plan.md`](plans/2026-09-12-opencode-heavy-lifting-plan.md). |
| Quad-Graph Exploration & Comparative Analysis Suite (OpenCode) | Done | All 4 streams executed and dossiers written: 1. `graph-knowledge-explorer` -> `graphify-out/foe-info/findings/2026-09-12-foe-info-modern-architecture.md`, 2. `foe-info-original-comparator` -> `graphify-out/foe-info-original/findings/2026-09-12-v1-baseline-comparison.md`, 3. `forge-hammer-comparator` -> `graphify-out/forge-hammer/findings/` + sibling, 4. `low-tool-comparator` -> `graphify-out/low-tool/findings/` + sibling. Synthesized into `graphify-out/foe-info/findings/2026-09-12-quad-graph-executive-synthesis.md`. See [`docs/plans/2026-09-12-quad-graph-exploration-and-comparison.md`](plans/2026-09-12-quad-graph-exploration-and-comparison.md) and the tracked [`docs/plans/2026-09-12-targeted-surgical-roadmap.md`](plans/2026-09-12-targeted-surgical-roadmap.md). |
| Deep Knowledge Graph 5-Repo Reindexing (DeepSeek) | Done | Re-indexed all 5 knowledge graphs (`FoE-Info-Extension`, `metadata-store`, `forge-hammer`, `LoW-Tool`, `FoE-Info-Extension-original`) using the DeepSeek backend with zero `llama-swap`/local VRAM usage. Labeled communities across all repos: FoE-Info (2,573 nodes, 227 communities), metadata-store (5,356 nodes, 52 communities), forge-hammer (523 nodes, 35 communities), LoW-Tool (662 nodes, 43 communities), and FoE-Info-original (402 nodes, 19 communities). Visual exports (wiki, callflow, obsidian, svg, html, tree) regenerated cleanly across all 5 repos. |
| Modernization Phase 4: Helper Formatter & GBG Renderer Extraction (OpenCode Agent 2) | Done | Brief premise corrected: `fRound`/`fNumber`/`fFormatNumber`/`fAgestring` never existed in `helper.js` (verified across all branches, the v1 baseline, and LoW-Tool). Added them as new pure null-safe utilities in `src/js/utils/formatters.js` (`fRound` fixed-decimal, `fNumber` separator-tolerant coercion, `fFormatNumber` locale grouping, `fAgestring` camel→spaced) and re-exported all four from `helper.js`. Extracted the ~180-line GBG changes renderer (`fshowBattleground`/`fshowBattlegroundChanges` + `setHeight` + resize observer) into `src/js/ui/renderBattlegroundsPanel.js` with `createLogger('BattlegroundsPanel')`, re-exported for full backward compatibility. `helper.js` 402 → 210 lines (< 250 target). Updated two source-coupled suites to read the new module; added `tests/fn/helper-modernization.test.mjs` and expanded `tests/utils/formatters.test.mjs`. Verification: `npm run verify` exit 0 (817/817 tests, prettier clean, eslint 0 errors, dev build compiles). |
| Modernization Phase 3: `index.js` Monolith Decomposition (OpenCode Agent 1) | Done | Extracted index-level UI/startup/runtime bindings into new `src/js/ui/indexUiBindings.js` (`createLogger('IndexUiBindings')`): `#go-to-options` trigger, `prefers-color-scheme` theme listener, `window` message bridge, storage byte-usage logging, storage/i18n bootstrap, runtime lifecycle (`onInstalled`/`onUpdateAvailable`/`requestUpdateCheck`), storage-change wiring, and the DevTools/network bridge. `src/js/index.js` dropped **1,003 → 566 lines** (≤ 600 cap) after removing dead code (clipboard block, `onClickHandler`, `onEvent`, `storageChange`, unreachable permission branch, unused `clear*` helpers and imports). Added `tests/ui/index-ui-bindings.test.mjs` (7 tests) and updated the source-coupled assertion in `tests/msg/guild-battleground-signals.test.mjs` to the new module. Verification: 804/804 tests, `npm run check` clean, `npm run lint` 0 errors (146 warnings), `npm run build:dev` compiles. |
| Modernization Phase 3 Track C: Calc TS Ports (Antigravity) | Done | Added strict typed ports `src/js/calc/eraMapping.ts` (114 lines; EraKey, EraAbbreviation, era mappings, numAges) and `src/js/calc/utils/spatialUtils.ts` (250 lines; GridCoordinate, SpatialBounds, calculateBoundingBox, manhattanDistance, euclideanDistance, areCoordinatesEqual, isWithinBounds) alongside runtime JS modules. Added unit test `tests/calc/era-spatial-types.test.mjs` (2 tests). `npx tsc --noEmit` exit 0, full verification gate green. |
| Slice 4 Track A: Storage Isolation & Startup Thinning (OpenCode Agent 1) | Done | F26: collapse flags now persist per-world under `world:<id>.collapses` via new `setCollapse`/`getCollapse` in `storage.js`; `saveWorldSettings` merges `collapses`; `storageListener` restores world-scoped state and legacy flat `collapse*` keys migrate to `world:en7` and are cleaned. F27: removed the flat `toolOptions` dual-write and made the world-scoped value authoritative so stale flat keys can no longer override panel heights. Thinned `StartupService.js` 600 → 501 lines by extracting `StartupCityStatsAggregator.js` (FP + goods tally + goods HTML) and moving the metadata render subscription into `StartupRenderOrchestrator.subscribeMetadataRenders`. Added `createLogger('CastleSystemService')` and `tests/msg/castle-system-service.test.mjs`. Verification: 792/792 tests, `npm run check` clean, `npm run lint` 0 errors, dev build compiles. |
| Modernization Phase 2: Calc TS Ports & F12 GB Map Consolidation (OpenCode Agent 2) | Done | Added strict typed mirrors `src/js/calc/BlueGalaxyCalculator.ts` (GalaxyCandidate/GalaxyEntityProduction/GalaxyOptions, lazy `createLogger('BlueGalaxy')`) and `src/js/calc/InvestedCalculator.ts` (investment entries, half-up Arc rounding, safe-spot summing, lazy `createLogger('InvestedCalc')`) alongside their shipping `.js` runtimes. Consolidated F12: `GB_NAME_MAP` + `getGreatBuildingName` now live canonically in `src/js/calc/gbNaming.js` with `GB_FALLBACK_NAMES` derived from it; `src/js/calc/utils/gbNames.js` is now a re-export shim, so `GreatBuildingRegistry.js` is untouched. `bignumberUtils.ts`/`eraUtils.ts` typecheck clean. Added `tests/calc/gb-names-consolidation.test.mjs` (4 tests). Evidence: `npx tsc --noEmit` exit 0, `npm test` 776/776, `npm run check` clean, `npm run lint` 0 errors, `npm run build:dev` compiles (branch `feat/opencode-modernization-calc`). |
| Slices 1-3 Heavy Lifter: F5/F6/F18/F19/F21/F23 (OpenCode) | Done | Extracted GBG signal payload resolution and list mutation into `src/js/msg/GbgSignalPayloadHandler.js` (`GuildBattlegroundService.js` 857 → 596 lines, ≤ 600 cap); reconciled `cardVisibility.ts`, `panelDispatcher.ts`, `GbgCalculator.ts`, and `GreatBuildingCalculator.ts` with their shipping `.js` twins; added `if (!item) continue;` to `getAllBoosts` and BigNumber accumulation in `applyBoostsToCity`; added popover `tabindex="0"` and converted informational cards to `role="status" aria-live="polite"`. Verification gate green (`1343183`, branch `feat/opencode-slices-heavy`). |
| Blocker Remediation Track A: GBG RPC Guards (OpenCode) | Done | Guarded partial/empty/null RPC payloads in `GuildBattlegroundService.getBattleground`, `getState`, and `getPlayerLeaderboard`: optional-chained `map.id`, normalized `map` province arrays with null-entry filtering, guarded `stateId`, `Array.isArray` defaults for leaderboard arrays, and `entry?.player?.name || 'Unknown'` fallbacks. Added `tests/msg/guild-battleground-guards.test.mjs` (5 tests). Verification gate green (`6019d29`, branch `fix/gbg-rpc-guards`). |
| Blocker Remediation Track B: QI Boosts & DOM ID (Antigravity) | Done | Resolved F17 & F20 (mapped real InnoGames server QI economy boost strings in `MilitaryBoostCalculator.js` and updated test fixtures) and F24 (namespaced `#guildText`, `#guildTextLabel`, and `#guildicon` to `#guildOverviewText`, `#guildOverviewTextLabel`, and `#guildOverviewIcon` in `renderGuildPanel.js` and `collapse.js`). Verification gate green (763/763 tests passed, check/lint clean). Merged into `development` (`b8994e7`). |
| GB Naming Extraction from helper.js (Antigravity) | Done | Moved `fGBsname` (short abbreviations) and `fGBname` (landmark and definitions resolution) from `src/js/fn/helper.js` into `src/js/calc/gbNaming.js` (266 lines) with dual CJS/ESM exports and 100% backward compatibility re-exports; `helper.js` dropped from 592 -> 408 lines (-184 lines, <= 475 target met). Updated `tests/fn/entity-lookup-logging.test.mjs` and expanded `tests/calc/gb-naming.test.mjs`. Full verification gate green (759/759 tests). |
| GBG Target Generator Extraction (OpenCode) | Done | Extracted `checkProvinces()` target generator DOM assembly, signal matching, and attrition token loop from `GuildBattlegroundService.js` into `src/js/ui/renderTargetGeneratorCard.js` (409 lines; `buildTargetGeneratorTargets` + `renderTargetGeneratorPanel`, `createLogger('GbgTargetGen')`). Placed in a new module to keep both under the 600-line cap; `gbgProvinceView.js` 383 -> 269 lines. `GuildBattlegroundService.js` dropped from 980 -> 848 lines (-132 lines). Added 4 tests to `tests/ui/gbg-province-view.test.mjs`. Full verification gate green. |
| GreatBuildingsService Decomposition (Antigravity) | Done | Extracted `showGreatBuldingDonation()` and place table loop into `src/js/ui/renderGbDonationPanel.js` (492 lines) with `createLogger('GbDonationPanel')`. `GreatBuildingsService.js` dropped from 736 -> 458 lines (-278 lines, <= 600 hard ceiling satisfied). Cleaned up dead variables and unused imports. Added `tests/ui/render-gb-donation-panel.test.mjs` (6 tests) and updated source-checking assertion in `tests/msg/great-buildings-options.test.mjs`. Full verification gate green (745/745 tests). |
| GuildBattlegroundService Decomposition (OpenCode) | Done | Extracted `getState(msg)` Battleground Result card into `src/js/ui/renderBattlegroundResultCard.js` (217 lines) with `createLogger('GbgResultCard')`; `getState` resolves the target container, delegates rendering, and pushes `BattlegroundPerformance` via the `onRow` callback. Service 1032 -> 980 lines (-52 lines). Added `tests/ui/render-battleground-result-card.test.mjs` (6 tests); updated source-coupled assertions in `guild-battleground-signals` and `panel-resize-and-visibility`. Full verification gate green (745/745 tests). |
| Monolith Decomposition Phase B (Antigravity) | Done | Extracted `EmissaryService.js`, `gbNaming.js`, `StartupBoostCoordinator.js`, and `cityStatsHtmlBuilder.js` from `StartupService.js`; dropped line count from 804 to 600 lines (<= 600 hard ceiling satisfied). Full verification gate green (739/739 tests). |
| Era Mapping Extraction (OpenCode) | Done | Extracted `fLevelfromAge`, `fAgefromLevel`, `fGVGagesname`/`fEraAbbreviation`, and `numAges` from `helper.js` into pure `src/js/calc/eraMapping.js` (Map lookups), re-exported from `helper.js` for full backward compatibility; `helper.js` 758 -> 592 lines. Added `tests/calc/era-mapping.test.mjs` (11 tests) and verified 1:1 behavioral parity against the pre-extraction source. Verification gate green (739/739). |
| Town Hall / Beta Debug Panel (OpenCode) | Done | Removed inline `max-height`/`overflow` overrides and the dual collapse listener from `betaDebugPanel.js`; Bootstrap now solely owns the collapse while icon state syncs from `hidden/shown.bs.collapse`. Bound height persistence to `beta:height` (250px default) via `bindResizableCollapse`. Added `tests/ui/beta-debug-panel.test.mjs`; verification gate green (716/716). |
| Codebase audit quick fixes | Done | Implemented verified quick fixes from multi-agent audit: `RewardRenderer.js` and `BonusService.js` collapse label and icon event binding parity, `AddElement.js` duplicate global keydown listener guard (`_foeA11yBound`), `CityProductionService.js` defensive property and state guarding, `ConversationService.js` numeric Unix epoch timestamp formatting via `dateUtils.formatTime`, `renderGbInfoPanel.js` dead code cleanup, and `gbDonationTables.js` copy ID deduplication. |
| Context View Filtering & 15-Panel Layout | Done | Implemented dynamic context view filtering in `cardVisibility.js` (GBG map view restricts to 6 combat panels, City view hides GBG panels, debug mode forces all 15 panels visible with debug placeholder stubs), created standalone `renderIncidentsPanel.js` decoupled from Harvest, created `renderHeaderPanel.js` integrating Player Points and City Boosts (Arc, CF, Coins, Supplies), normalized player score in `accountParser.js` + `StartupService.js`, and established strict 15-panel vertical mounting hierarchy in `containerBinding.js`. |
| DevTools teardown delay | Done | Eliminated 1.7–2.5s freeze when closing DevTools; guarded flushCityEntityDefs to only run when dirty, stripped 30MB unmodified metadata writes (AllyDefs, BuildingEntityLookup) on unload, and cleaned up panelWindow references in devtools.js on panel.onHidden/unload (`39e3b10`). |
| Own-city GB cache registration | Done | Fixed own-city Great Building lookup miss and foreign GB sticking when opening own GB by clicking inside city; registered city map Great Building entities in `GreatBuildingRegistry` and handled player ID fallback in `legacyBridge.js`. |
| GB and GBG panel display order | Done | Enforced panel order for Great Buildings (1. GB Donation, 2. GB Info, 3. GB Contributors) and GBG (1. Target Generator, 2. Battlegrounds Changes, 3. GBG Leaderboard, 4. rest) in `containerBinding.js` and `GreatBuildingsService.js`. |
| Custom panel resize retention | Done | Created `panelResize.js` (`bindResizableCollapse`) preventing panels from expanding to full content size on collapse/expand; removed `max-height: max-content !important` from `.resize`; preserved user drag size in world storage across collapse toggles for Army, Goods, and Treasury panels. |
| Army panel sizing & persistence | Done | Calibrated default Army Panel height to 185px (`#armyText`, outer card 229px) across `globals.js`, `factoryDefaults.js`, and `ArmyUnitManagementService.js`. Added `.collapsing` and `!show` guards to `ResizeObserver` preventing animation frames from corrupting stored height; verified per-world storage persistence (`3b167a3` + update). |
| Battlegrounds collapse performance | Done | Fixed lag/freeze on Battlegrounds panel collapse: scoped `min-height: 250px` to `.gbg-full-roster.show` and disabled collapse transitions on `.collapsing` in `custom.scss` to eliminate frame-by-frame 350ms table reflow; added observer guards in `helper.js`. |
| Lists Copy button alignment | Done | Fixed Friends, Guild, and Hood player lists Copy buttons overlapping container borders; replaced absolute offset with flex header rows (`d-flex flex-row justify-content-between align-items-center mb-0`) in `OtherPlayerService.js` and updated `collapse.js`. |
| GE Championship server alignment | Done | Left-aligned Server column header and cell text (`text-start`) in `expeditionTables.js` and `custom.scss` (`#geChampionshipTable`). |
| GE Leaderboard styling parity | Done | Styled GE Leaderboard (`#geContributionTable`) to match GBG Leaderboard: `.goods-table` table styling, text-start for members, text-center for trial, and centered formatted numbers with `tabular-nums`. |
| Panel layout ordering | Done | Organized DevTools `#content` container into a clean 5-group player workflow in `containerBinding.js` (City -> Military -> Great Buildings -> Guild Activities -> Social/System). |
| Table header capitalization | Done | Capitalized `Type` and `Amount` across all 7 language dictionaries in `src/i18n/` for Goods Inventory and Guild Treasury consistency. |
| Goods & Treasury table alignment | Done | Removed artificial `ps-3` indentation from item cells in `ResourceService.js`, `panelDispatcher.js`, and `OutpostService.js`, aligning goods and resource rows flush with table headers and era titles (`5608b43`). |
| Own City Info card non-dismissible | Done | Preserved player's own City Info overview as permanently visible by removing close button and `alert-dismissible` from `ownCityCard.js` and `renderCityStats.js` (`5c2dbf1`). |
| GB donation place headers | Done | Reverted GB donation place titles to classic "1st Place", "2nd Place" etc. by removing accidental `(${donorArcPercent}% Arc)` suffix (`53b89c0`). |
| Universal panel collapsibility & controls | Done | Made all panels collapsible on title click with `[-]`/`[+]` toggles and close buttons. Created dedicated "Other Player Information" header with player link on line 1 (`c1c8a00`). |
| GB closed panels reopen fix | Done | Retained `#greatbuilding`, `#gbInfo`, `#donation2` containers in DOM upon close so opening subsequent Great Buildings re-mounts and displays them properly (`fbe4b4a`). |
| CF Bonus line placement | Done | Positioned Chateau Frontenac (CF) bonus on its own line directly under Arc bonus in `ownCityCard.js` and `visitedCityCard.js` (`b19c2eb`). |
| Options page instant render | Done | Eliminated blank delay and unpopulated FOUC in options.html; populated form synchronously from storage; moved tab discovery to non-blocking background; added unit test. |
| Release v0.0.835 | Done | Context-aware view filtering (GBG 6-panel mode, City view, Debug mode override with stubs), standalone Incidents extraction, 15-panel stacking hierarchy, player score normalization, and audit quick fixes (`RewardRenderer`, `BonusService`, `AddElement`, `CityProductionService`, `ConversationService`, `renderGbInfoPanel`, `gbDonationTables`). Verification gate passed (683/683 tests), WebStore zip built, GitHub Release published. |
| Release v0.0.834 | Done | Codebase audit fixes (detached DOM nodes, tooltip IDs, card visibility, popover listeners, RPC guards, BigNumber edge cases), verification gate passed (661/661 tests), WebStore zip built, git tag v0.0.834 pushed, GitHub Release published. |
| Release v0.0.833 | Done | Bumped version to 0.0.833, created CHANGELOG.md, added scripts/release.mjs, built WebStore zip, created git tag v0.0.833. |
| GBG UC & rushed camps | Done | Formatted UC camps as `(60% / 20% UC)`. Reconciled server `gainAttritionChance` in `GbgCalculator.js` so rushed camps promote to ready immediately rather than showing stale UC (`bbd25b9`). |
| GBG instant conquest signals | Done | Registered `GuildBattlegroundService.getAction` (`province_conquered`) in `legacyBridge.js`; conquered sectors clear from target generator instantly via WebSocket push instead of ~50s poll lag (`bbd25b9`). |
| GBG panel sizing & initial height | Done | Adapted GBG panel sizing: default restricted height increased to 400px; changes-only mode sets `height: auto` (`.gbg-changes-full`), preventing panel starting off cramped (`bbd25b9`). |
| Passive network interception | Done | Restored `xhrInterceptor.js` to 100% passive observation; removed out-of-band active POST RPC fetches that interfered with player's live session and message threads. |
| Custom targets topic matching | Done | Dynamically resolves custom target topic from options/state in `ConversationService.js` with fallback to `targets` / `🎯` without active background querying. |
| Teaser payload shapes support | Done | Supported all teaser payload shapes (array items with text or flat objects) in `ConversationService.js` (`c3756e5`). |
| Content bridge role confirmed | Done | Verified `contentBridge.js` is essential MV3 network transport; confirmed Claude log mirroring was already removed in `981180d`. |
| Restore using-superpowers skill | Done | Recreated `.agents/skills/using-superpowers/SKILL.md` + references for `/using-superpowers` slash command (53 skills total, verified 40/40 agent tests). |
| Protocol paths & webpack proxy | Done | Relocated `xhrInterceptor.js` and `contentBridge.js` directly to `src/js/protocol/`; created root `webpack.config.js` proxy. |
| GE panels rename & color | Done | Renamed Championship/Contributions to GE Championship and GE Leaderboard; updated i18n, options, and inherited alert-info font color on table elements (verified 602/602 tests). |
| GB donation panel refresh | Done | Fixed real-time progress update on FP donation (`GbDonationService.updateContributionProgress` + `CityMapService.updateEntity` sync); live browser verified on en7 Zeus 2026-09-11. |
| Codex/Claude residue removal | Done | 3 codex worktrees deleted; `codex-sync.log` deleted; no `codex/*` branch refs remain (`git branch -a` clean as of 2026-09-11). Historical decommission mentions in docs intentionally retained. |
| Safety-gate branch-delete policy | Done | `.agents/scripts/safety-gate.mjs` now blocks deleting `development` only (local + remote); all other branch deletes allowed. Tested 21/21. |
| Docs consolidation | Done | Created `docs/README.md` hub + this board; plans/specs migrated to `docs/plans/` + `docs/specs/`; AGENTS.md/COORDINATION.md/OPENCODE.md/HANDOFF.md all reference the hub. |
| `.opencode/` lockfile | Done | `.opencode/package-lock.json` tracked in `089e655`. |
| Verification gate | Done | `npm run verify` exit 0 confirmed 2026-09-11 (624/624 tests, prettier clean, lint 0 errors, dev build compiles). Reproof after any new change. |
| Graph pipeline extension | Done | Scripts + npm runners + MCP servers for `graphify-low-tool` and `graphify-foe-info-original` (sibling repos); repointed original graph; fixed forge-hammer sibling path bug; added `graphify-out/` gitignore in both sibling repos. |
| 5 new graph subagents | Done | `low-tool-comparator`, `foe-info-original-comparator`, `forge-hammer-kg-explorer`, `low-tool-kg-explorer`, `foe-info-original-kg-explorer` (canonical + opencode shims). 36 subagents total (`b3e2875`). |
| Ecosystem count sync | Done | AGENTS.md, graphify rule, antigravity-interop skill, guardrail, utils, docs (`STATUS`/`COORDINATION`/`OPENCODE`/`HANDOFF`) and agent-config tests updated to 36 subagents / 6 MCP servers / 53 skills. +10 `mcp(graphify-low-tool/*)` Antigravity grants. |
| Chrome types & hybrid TS config | Done | Installed `@types/chrome` (^0.2.9) in devDependencies, configured `tsconfig.json` for gradual adoption (`allowJs`, `checkJs: false`, `strict: false`, chrome/webextension-polyfill/node types), scoped Chrome extension globals (`globals.browser/chrome/webextensions`) to `src/**/*.{js,mjs,cjs}` in `eslint.config.mjs`; `npx tsc --noEmit` clean (`a5c5b96`, merged `9e63acb`). |
| UI visibility/dispatcher TS mirrors | Done | Added typed `src/js/ui/cardVisibility.ts` (`ViewState`, `PanelId` union for all 15 panels, `ShowOptionsState`) and `src/js/ui/panelDispatcher.ts` (containers, treasury deps) mirroring the `.js` runtimes 1:1; verified jQuery-free (vanilla DOM only); `.js` callers and tests untouched; `tsc --noEmit` clean and full `npm run verify` green (`ceef68d`, merged `d7dd28d`, 704/704 tests). |
| SocialState + GB donation inactive fix | Done | `ui/gbDonationTables.js` snapshot `OtherPlayerService.friends`/`guildMembers`/`hoodlist` at module load while the service reassigns them (`OtherPlayerService.js:192-194`), so `checkInactive` read stale/empty lists. New `state/SocialState.js` (`lists` channel) published by `OtherPlayerService`; `checkInactive` reads live lists. Removes a `ui/ → msg/` require edge. Tests: `tests/state/social-state.test.mjs` + `gb-donation-tables` live-read assertion. Verification: `npm run verify` exit 0 — 1,284 tests / 0 fail. |
| cardVisibility config extraction | Done | Moved the frozen tables out of `ui/cardVisibility.js` (739 → 503 L) into new `ui/cardVisibilityConfig.js` (272 L); `cardVisibility.js` imports them and keeps its back-compat re-exports; engine unchanged. `.ts` mirror re-export deferred to the `.js`/`.ts` canonical-direction decision. Verification: `npm run verify` exit 0 — 1,284 tests / 0 fail, `tsc --noEmit` clean. |

## Todos

- [x] Live visual verification (closed 2026-09-12 by user directive): user runs visual checks continuously, so the dedicated task is not needed; code verification via `npm run verify` suffices.
- [ ] Modern-web Tier 3 (deferred, higher risk): migrate Bootstrap popovers/collapse to native `popover` + CSS Anchor Positioning, adopt `light-dark()`/`[data-bs-theme]` theming, and evaluate opt-in Built-in AI/WebMCP enhancements.
- [x] Modern-web `panel.html <main>` landmark (closed 2026-09-12): panel content is now created as a `<main>` element in `src/js/index.js` (JS-created, since `panel.html`'s body is empty), giving the DevTools panel a single main landmark.
- [ ] Modern-web deferred items: `content-visibility` on measured card bodies (no safe stable selector yet), `MessageDispatcher` parse yielding, and residual table semantics — `renderBattlegroundsPanel.js` already has `<caption>`/`scope="col"`; the `renderInvestedPanel.js` table is a commented-out future feature.
- [ ] Graph-explorer follow-ups (2026-09-12) — full ranked backlog in [`docs/plans/2026-09-12-post-f2-refactor-backlog.md`](plans/2026-09-12-post-f2-refactor-backlog.md): (a) have the `.ts` mirror `ui/cardVisibility.ts` re-export `ui/cardVisibilityConfig.js` (the `.js` extraction shipped in Batch 18); (b) review the 4 lazy `resolveDep` `ui/ → msg/` fallbacks in `ui/indexUiBindings.js` (F3 left them, injectable from `index.js`); (c) dead-code cleanup: delete `fn/cityStatsUtils.js` (0 consumers) and decide `ui/cityStatsHtmlBuilder.js` (production-orphan); (d) decide canonical direction for the 12 hand-maintained `.js`/`.ts` mirror pairs; (e) monolith extracts: `MessageDispatcher` direct-metadata router (~64 L), `GreatBuildingsService.fCheckOutput` (~83 L), `containerBinding.setupPanelContainers` (~277 L), `fn/collapse.js` toggle factory.
- [x] C3 — BG/sniping UX (closed 2026-09-12 by user directive): sniping UX already shipped (calculators sorted); BG enhancements may return later but are not a current priority.
- [x] P1 — RPC contract gate (`feat/opencode-rpc-contract`): eliminated all 14 duplicate registrations (modern `src/js/msg/*Service.js` authoritative; legacy `protocol/routes/*` defers) and enforced the gate. `scripts/rpc-contract.config.json` now categorizes out-of-scope `ignoredCaptures` (21 classes / 25 keys) and triaged `allowedUnhandled` (20 keys) with `knownDuplicates: []`; `--check` exits non-zero on untriaged unhandled or un-allowlisted duplicates and runs first in `npm run verify` via `rpc:contract:check`. Architecture analysis in [`docs/specs/2026-09-12-payload-driven-architecture-improvements.md`](specs/2026-09-12-payload-driven-architecture-improvements.md).
- [x] P0 — GB package balance & level-closing profitability view (`feat/antigravity-gb-fp-packages`): added pure `calculateLevelClosingProfit` to `GreatBuildingCalculator.js`, wired `GreatBuildingsService.getAvailablePackageForgePoints`, FP packages balance badge, and level-closing profit alert in `renderGbDonationPanel.js`. Merged in `07a478b`.
- [x] GB donation + rewards routing continuation (v1-aligned): extracted `src/js/ui/rewardCategories.js` (160L), unified single-source `showReward` categorization, decoupled `CityProductionService.js` and `QuestService.js`, simplified `RewardRenderer.js` (281 -> 194 lines), and added `tests/ui/reward-routing.test.mjs`. Merged in `c4f9e98`.
- [x] QI implementation Slice 1: add `guildRaidsRoutes.js`, `quantumState.js`, and `GuildRaids*Service.js` CJS parsers wired into the dispatcher (`e2afaa6`, `55d99b4`).
- [x] Modernization Phase 5: `index.js` orchestrator thinning (566 lines -> 165 lines) via modular bridge and entity defs extraction (`eeaa9e6`, `b80cc58`).
- [x] Actionable Item 1 (Knowledge Base): Shipped 2026-09-12 — extracted `_dedupCache`/`isDuplicate`/`clearDedupCache` into `src/js/protocol/dedupCache.js` (73 L, `createLogger('DedupCache')`); `MessageDispatcher.js` 607 -> 583 L; regression `tests/protocol/dedup-cache.test.mjs` (8 tests).
- [x] Actionable Item 2 (Knowledge Base): Shipped 2026-09-12 — slice 1: `GuildRaidsService` publishes to the reactive `QuantumState` and `renderQuantumPanels` self-subscribes; slice 2: `StartupService` publishes `city-stats`/`building-collection` context to `StartupRenderState`, consumed by `ui/startupRenderBinding.js`.
- [x] F6 (reactive-store graph audit): Shipped 2026-09-12 — `notify()` now logs subscriber failures via an injectable scoped logger instead of swallowing them. Findings recorded in `graphify-out/foe-info/findings/2026-09-12-reactive-store-migration-audit.md`.
- [x] F5 standardize the reactive binding convention (closed 2026-09-12): convention is a dedicated `ui/<domain>RenderBinding.js` module exposing `bindX(state, { renderers })`, loaded for side effect by `index.js`; render modules stay pure. Slice 1 refactored to match — new `ui/quantumRenderBinding.js` (`bindQuantumPanels`), self-subscription removed from `renderQuantumPanels.js`. All F2 migrations must follow this shape.
- [x] F3 remove the `ui/ → msg/` inversions (closed 2026-09-12): `panelDispatcher.js`/`.ts` now read `ResourceDefs` from `state/state.js` (guarded by `tests/ui/panel-dispatcher-state-purity.test.mjs`); `indexUiBindings.js` top-level msg requires moved to the module's lazy `resolveDep` injection pattern. Refreshed AST shows **0 production `ui/ → msg/` static import edges**. Residual lazy service-action requires (gbDonationTables, indexUiBindings bootstrap triggers) are addressed by F7.
- [x] F2 batch-migrate the remaining `msg/` services off direct `msg/ → ui/` renderer imports. **Batch 1 (2026-09-12):** `EmissaryService` → `StartupRenderState.requestCityStatsRepaint()`, zero `ui/` imports. **Batch 2 (2026-09-12):** `TreasuryService` → new `state/TreasuryState.js` + `ui/treasuryRenderBinding.js`, zero `ui/` imports; `renderTreasuryPanel` re-export removed. **Batch 3 (2026-09-12):** `BonusService` → new `state/BonusState.js` + `ui/bonusRenderBinding.js`, zero `ui/` and zero `StartupService` imports (uses `blueGalaxyState.setCharges`). **Batch 4 (2026-09-12):** `ArmyUnitManagementService` → new `state/ArmyState.js` + `ui/armyRenderBinding.js`, zero `ui/` imports; DOM test migrated to import the binding. AST `msg/ → ui/` static edges 29 → 21. **F2-prep (2026-09-12):** GE parsers extracted to `src/js/parsers/expeditionParser.js` (ui re-exports for back-compat). **Batch 5 / plan Session 0+3 (2026-09-12):** created `ui/renderBindings.js` composition root (F7); `GuildBattlegroundService` → new `state/GuildBattlegroundState.js` (channels `province`/`result`/`leaderboard`/`targets`) + `ui/gbgRenderBinding.js`; `GbDonationService` → new `state/GbDonationState.js` (`donation`/`reward` channels) + `ui/gbDonationRenderBinding.js`, with the unused `renderGbDonationPanel` re-export dropped and `handleNewReward` tests asserting the store payload; both services have zero `ui/` imports. Store/binding suites added; source-coupled assertions migrated. **Batch 6 / plan Session 2 (2026-09-12):** `GreatBuildingsService` → new `state/GreatBuildingsState.js` (`donors`/`info`/`donation`) + `ui/greatBuildingsRenderBinding.js`, registered in `renderBindings.js`; the unused `renderGbDonationPanel` re-export dropped; zero `ui/` imports. **Batch 7 / plan Session 4 (2026-09-12):** `GuildExpeditionService` → new `state/ExpeditionState.js` (`international`/`contribution` channels + `reset()`) + `ui/expeditionRenderBinding.js`; combined HTML builder moved to `ui/expeditionTables.js` (`buildExpeditionContentHtml(entries, entries, options)`); the module-level caches and `../ui/` imports are gone, `resetExpeditionCache()` now delegates to `expeditionState.reset()`. Tests: `tests/state/expedition-state.test.mjs`, `tests/ui/expedition-render-binding.test.mjs`; `guild-expedition-trial` migrated off the `?t=` cache-buster. **Batch 8 / plan Session 5 (2026-09-12):** `CityProductionService`, `QuestService`, and `InvestedService` decoupled. New shared `state/RewardState.js` (`reward` channel) + `ui/rewardRenderBinding.js` route City/Quest rewards through the unified `showReward` (single subscription avoids the double-render risk); new `state/InvestedState.js` (`contributions` channel) + `ui/investedRenderBinding.js`. `QuestService` now takes `deps.rewardState` (replacing `rewardRenderer`); `CityProductionService` dropped its `fn/RewardRenderer` shim import; all three have zero `ui/` imports and no `../ui/` edges. Tests: `reward-state`, `invested-state`, `reward-render-binding`, `invested-render-binding` suites plus migrated `reward-routing`/`render-bindings`. Verification: `npm run verify` exit 0 — 1,232 tests / 0 fail. Remaining: Outpost, Resource, StartupRenderOrchestrator, StartupService — batched via subagents. **Batch 9 / plan Session 6 (2026-09-12):** `OtherPlayerService` own-city score repaint now routes through `startupRenderState.requestCityStatsRepaint()` (mirrors `EmissaryService`) instead of lazily requiring `ui/renderLiveCityStats.js`; the service has zero direct `../ui/` imports. Deferred: extracting the visited-card/list DOM rendering (which flows through the legacy `fn/renderCityStats.js`/`fn/AddElement` shims) behind `VisitedCityState` — a larger DOM extraction tracked as a follow-up. Verification: `npm run verify` exit 0 — 1,234 tests / 0 fail. **Batch 10 / plan Session 7 (2026-09-12):** `OutpostService` → new `state/OutpostState.js` (`cultural` channel) + `ui/outpostRenderBinding.js`; the three `renderCulturalPanel(...)` calls now publish via `publishCulturalPanel()`, and the service's `renderCulturalPanel`/`setShowOptions` re-exports were dropped (the test imports them from `ui/renderCulturalPanel.js`). Zero direct `../ui/` imports. Tests: `tests/state/outpost-state.test.mjs`, `tests/ui/outpost-render-binding.test.mjs`; `render-bindings` lists `outpostState`. Verification: `npm run verify` exit 0 — 1,244 tests / 0 fail. **Batch 11 / plan Session 8 (2026-09-12):** `ResourceService` → new `state/ResourceState.js` (`goods`/`fp`/`globals`/`clear` channels) + `ui/resourceRenderBinding.js`; goods renders, available-FP, renderer globals, and the dismiss/clear signal now publish, with `onCopy`/`onDismiss` injected by the binding. The goods lock API (`isGoodsPanelUnlocked`/`unlockGoodsPanel`/`lockGoodsPanel`) and market-trigger behavior are preserved; zero direct `../ui/` imports. Tests: `tests/state/resource-state.test.mjs`, `tests/ui/resource-render-binding.test.mjs`; `resource-market-trigger` side-effect-loads the binding; `render-bindings` lists `resourceState`. Verification: `npm run verify` exit 0 — 1,257 tests / 0 fail. **Batch 12 / plan Session 9 (2026-09-12):** `StartupRenderOrchestrator` → extended `state/StartupRenderState.js` with a `metadata-loading` channel; new `ui/startupMetadataLoadingBinding.js` renders the placeholder. The orchestrator's lazy `ui/startupMetadataLoading.js` require is gone (zero direct `../ui/` imports); the two barrier/deferred tests side-effect-load the binding. Tests: `tests/ui/startup-metadata-loading-binding.test.mjs` plus the extended startup-render-state suite. Verification: `npm run verify` exit 0 — 1,263 tests / 0 fail. **Batch 13 / plan Session 10 (partial, 2026-09-12):** removed the dead `buildCityStatsHTML(...)` result in `StartupService` (assigned to `citystatsHTML` but never read), eliminating the `ui/cityStatsHtmlBuilder.js` edge, plus the unused `buildFpTooltipHTML`/`GameOrigin` imports and orphaned `diamonds`/`goodsHTML` locals. Remaining `StartupService` ui edges deferred to Session 10-11 follow-up: `cityStatsTooltips` (`showTooltips`), `components/cityStatsTooltipBuilder` (`buildTotalGoodsTooltipHTML`), `playerTooltip` (used + re-exported), `renderGalaxyPanel`, `renderLiveCityStats`. Verification: `npm run verify` exit 0 — 1,263 tests / 0 fail. **Batch 14 / plan Session 10 (2026-09-12):** moved `showTooltips` out of `StartupService` into `ui/startupRenderBinding.js` (now bound automatically after each city-stats paint, with an injected seam), removing the `ui/cityStatsTooltips.js` edge; also dropped the unused `renderGalaxyPanel` and `fGoodsText` named imports. Remaining `StartupService` ui edges (dedicated follow-up): `components/cityStatsTooltipBuilder` (`buildTotalGoodsTooltipHTML`), `playerTooltip` (used + re-exported), `renderGalaxyPanel` (`showGalaxy`/`updateGalaxy`), `renderLiveCityStats` (`buildClanGoodsData`/`fGoodsHTML`). Verification: `npm run verify` exit 0 — 1,264 tests / 0 fail. The final tail is planned in [`docs/plans/2026-09-12-f2-startup-service-final-decouple.md`](plans/2026-09-12-f2-startup-service-final-decouple.md) (Slice A: tooltip context + `playerTooltip` helpers via an `ignore-list` store channel; Slice B: galaxy wiring via `blueGalaxyState` + clan-goods aggregation move). **Batch 15 / plan Slice A (2026-09-12):** removed the `ui/components/cityStatsTooltipBuilder.js` edge by dropping `buildTotalGoodsTooltipHTML` and the never-consumed `tooltipHTML.totalGoods` from `StartupService` (the live renderer already computes it from `goodsList`); `getUserTooltipHTML`/`getScoreDBOrigin` now resolve inside `ui/renderLiveCityStats.js`; `updateIgnoreListUI` routes through a new `ignore-list` channel on `StartupRenderState` (`requestIgnoreListRefresh()`), consumed by `startupRenderBinding`, published by `OtherPlayerService`, with `protocol/indexBridgeSetup.js` importing it directly from `ui/playerTooltip.js`. All `playerTooltip` imports/re-export dropped from `StartupService`. Tests: extended `startup-render-state` + `startup-render-binding` suites. Verification: `npm run verify` exit 0 — 1,266 tests / 0 fail. Remaining `msg/ → ui/` edges: `ConversationService → AddElement` (expected) and `StartupService → renderGalaxyPanel`/`renderLiveCityStats` (Slice B). **Batch 16 / plan Slice B (2026-09-12) — F2 DoD met:** `showGalaxy`/`updateGalaxy` wiring moved to new `ui/galaxyRenderBinding.js` (registered in the barrel), with `StartupService` driving galaxy repaints via `blueGalaxyState.notify()` and `CityProductionService` calling `blueGalaxyState.updateEntity(reward)`; `fGoodsText`/`fGoodsHTML`/`buildClanGoodsData` moved to pure `calc/goodsTooltipFormatter.js` (re-exported by `ui/renderLiveCityStats.js`), removing the last `StartupService → ui/` import. `grep -rnE "from '\.\./ui/|require\('\.\./ui/" src/js/msg/` now returns only `ConversationService.js → ui/AddElement.js`. Tests: new `tests/ui/galaxy-render-binding.test.mjs` (3). Verification: `npm run verify` exit 0 — 1,269 tests / 0 fail, calc purity guard green, dev bundle compiles. **Batch 17 / plan Session 6 completion (2026-09-12):** built the deferred visited-city reactive store — new `state/VisitedCityState.js` (`visit` channel) + `ui/visitedCityRenderBinding.js` (registered in the barrel); `OtherPlayerService.renderVisit` publishes `{ containerId, stats, context, options }` and the binding renders/translates, so the service drops its `fn/renderCityStats.js` and `fn/i18n.js` imports. Tests: `visited-city-state`, `visited-city-render-binding`; `other-player-service` side-effect loads the binding. Verification: `npm run verify` exit 0 — 1,278 tests / 0 fail.
- [x] F7 replace load-bearing side-effect imports in `index.js` with explicit bootstrap calls so wiring is visible and testable (closed 2026-09-12): `src/js/ui/renderBindings.js` is now the single composition root — a CJS `require` barrel of the seven render bindings (`army`, `bonus`, `gbDonation`, `gbg`, `quantum`, `startup`, `treasury`) imported once by `index.js`; wiring is visible and covered by `tests/ui/render-bindings.test.mjs` (each singleton store gains a subscriber).
- [x] Actionable Item 3 (Knowledge Base): Shipped 2026-09-12 — `src/js/calc/CityStatsCalculator.ts` (393 L) + `src/js/state/MetadataStore.ts` (595 L) strict mirrors; `tsc --noEmit` clean; webpack still resolves the `.js` runtimes.
- [x] Actionable Item 4 (Knowledge Base): Shipped 2026-09-12 — added `src/js/protocol/devtoolsBridge.js` structured `postMessage` channel with a per-show readiness handshake; `devtools.js` and `index.js` no longer rely on the global `window.handleRawNetworkEntry` function attachment for the panel bridge; regression `tests/protocol/devtools-bridge.test.mjs` (8 tests).
- [x] GBG Target pruning on sector conquest by any guild (`GuildBattlegroundService.js`)
- [x] Restore pure passive observation in `xhrInterceptor.js`, eliminating active out-of-band POST RPC requests
- [x] Support multiple teaser payload shapes in `ConversationService.js` (`c3756e5`)
- [x] Confirm `contentBridge.js` architecture and historical Claude logger removal (`981180d`)
- [x] Restore `using-superpowers` skill for `/using-superpowers` slash command (53 skills total)
- [x] Relocate `xhrInterceptor.js` and `contentBridge.js` directly to `src/js/protocol/`
- [x] Add root `webpack.config.js` proxy for default tooling compatibility
- [x] Fix GB donation panel double-count on donate (`6df53f3`, unpushed; live spot-check pending)
- [x] Delete codex worktrees + `codex-sync.log` (2026-09-10)
- [x] Delete remaining `codex/*` branch refs (confirmed gone 2026-09-11)
- [x] Relax safety-gate branch-delete block (allow non-`development` local/remote deletes)
- [x] Migrate `docs/superpowers/plans` → `docs/plans/` (9 files)
- [x] Migrate `docs/superpowers/specs` → `docs/specs/`
- [x] Create `docs/README.md` coordination hub + `docs/STATUS.md` live board
- [x] Update `AGENTS.md`, `HANDOFF.md`, `OPENCODE.md`, `COORDINATION.md` pointers to the hub + new plan paths
- [x] Commit `.opencode/package-lock.json` (`089e655`)
- [x] Run `npm run verify` + graphify AST sync after consolidation completes (2026-09-11)
- [x] Push `development` (`6df53f3` + `5dae931`) to `origin/development` (2026-09-11)
- [x] Live browser spot-check: own GB donate → card refreshes without reopening (verified on en7 Zeus 2026-09-11)
- [x] Position Chateau Frontenac (CF) bonus on its own line under Arc bonus in City Overview (`b19c2eb`)
- [x] Fix GB closed panels (GB info, contributors, donation) to reappear upon opening Great Buildings (`fbe4b4a`)
- [x] Make all panels collapsible on title click with `[-]`/`[+]` icons and close buttons; "Other Player Information" header separation (`c1c8a00`)
- [x] Revert GB donation place headers to classic "1st Place", "2nd Place" (removed Arc bonus suffix) (`53b89c0`)
- [x] Keep player's own City Info overview non-dismissible without close button (`5c2dbf1`)
- [x] Align Goods Inventory, Guild Treasury, and Cultural Outpost tables flush with headers (removed `ps-3`) (`5608b43`)
- [x] Calibrate Army Panel default height to 185px (`#armyText`, outer card 229px) and guard ResizeObserver against collapse animation (`3b167a3` + update)
- [x] Fix Battlegrounds panel collapse stutter/lag (min-height scoping + collapse transition suppression)
- [x] Fix Lists Copy button border overlap via flex header alignment (`OtherPlayerService.js`)
- [x] Left-align Server column in GE Championship table
- [x] Match GE Leaderboard styling to GBG Leaderboard with tabular-nums and number formatting
- [x] Organize `#content` panel order into clean 5-group workflow (`containerBinding.js`)
- [x] Capitalize `Type` and `Amount` across all 7 language dictionaries for Goods & Treasury tables
- [x] Fix own-city Great Building cache miss and foreign GB sticking (`GreatBuildingRegistry.js`, `legacyBridge.js`)
- [x] Enforce GB panel order (Donation -> Info -> Contributors) and GBG panel order (Target Generator -> Changes -> Leaderboard) (`containerBinding.js`, `GreatBuildingsService.js`)
- [x] Retain custom panel resize dimensions across collapse/expand and prevent full-height blowup (`panelResize.js`, `custom.scss`)
- [x] Generate `../LoW-Tool/graphify-out/graph.json` via `npm run graph:low-tool:reindex` (completed 2026-09-11)
- [x] Generate `../FoE-Info-Extension-original/graphify-out/graph.json` via `npm run graph:foe-info-original:reindex` (completed 2026-09-11)
- [x] Relocate `metadata-store` to sibling `../metadata-store` with graphify output suite (`graph.json`, `wiki/`, `obsidian/`, `svg`, `html`, `tree`)
- [x] Align `../forge-hammer` peer repository to minimal graph-only setup matching `LoW-Tool` and `FoE-Info-original` (removed unneeded `.agents/`, `AGENTS.md`, `tests/`, `package.json`)
- [x] Decouple 11 technical specialist personas (`javascript-expert`, `typescript-expert`, `webpack-expert`, etc.) to general domain experts
- [x] Comprehensive codebase audit across `src/js/msg/`, `src/js/calc/`, `src/js/ui/`, and `src/js/protocol/`
- [x] Implement non-breaking quick fixes (detached `#visit` DOM nodes, Colonial Age tooltip ID, `#citystats` card visibility, Popover listener leaks, RPC payload guards, BigNumber edge cases)
- [x] Resolve audit blockers F14/F15/F16: defensive GBG RPC payload guards in `GuildBattlegroundService.js` (`6019d29`)
- [x] Monolith decomposition Phase A: Extract OutpostService.js (Cultural Settlements) and QuestService.js
- [x] Monolith decomposition Phase B: Extract EmissaryService.js from StartupService.js (Antigravity)
- [x] Town Hall / Beta Debug panel height & scroll behavior refactor (OpenCode, see docs/archive/plans/2026-09-11-town-hall-beta-debug-panel.md)
- [x] Slices 1–3 Remediation Track B (Antigravity): F8 (BigNumber fallback), F9 (JSDoc fix), F10 (BigNumber math), F25 (remove !important), F4 (hot-path purity), F7 (DTO normalization) (`5209ab0`)
- [x] Slices 1–3 Remediation Track A (OpenCode): F5 (GBG decomposition), F18/F19 (BoostService null guards & BigNumber), F6 (TS mirror sync), F21/F23 (a11y tabindex/role) (`1343183`, merged `a062bdf`)
- [x] Modernization Phase 2 (OpenCode Agent 2): typed `BlueGalaxyCalculator.ts`/`InvestedCalculator.ts` ports, F12 GB name map consolidation into `gbNaming.js`, and `bignumberUtils.ts`/`eraUtils.ts` parity (branch `feat/opencode-modernization-calc`)
- [x] Modernization Phase 4 (OpenCode Agent 2): new pure formatters in `utils/formatters.js` + `helper.js` re-exports; GBG changes renderer extracted to `ui/renderBattlegroundsPanel.js`; `helper.js` 402 → 210 lines; `tests/fn/helper-modernization.test.mjs` added (branch `feat/opencode-modernization-helper`)

## Recently completed threads

- **Quad-Graph Exploration & Comparative Analysis Suite** — DONE. Four parallel specialist streams mapped the host against the v1 baseline, Forge-Hammer, and LoW-Tool. Consensus: architecture is healthy and ahead of peers, with risk in four confirmed defects — (D1) `fResourceShortName` drops the runtime `ResourceNames` lookup (raw IDs render; untested), (D2) duplicate RPC ownership silently combined by `MessageDispatcher`, (D3) calc dependency impurity (`VisitedCityStatsCalculator` → `CastleSystemService`, `gbNaming` `globalThis`), (D4) orphaned `webRequestFilter.js`, (D5) TS/JS mirror drift outside the `verify` gate. Remaining monoliths: `legacyBridge.js` (831 L, 71 register calls) and `CityMapEntityProcessor.js` (657 L, one 610-L function). Feature gaps vs Forge-Hammer: Blue Galaxy economic ranking and the date/time engine. Plan premises corrected: v1 baseline line counts, v1 already used BigNumber, no Blue Galaxy probability model, and LoW-Tool is a FoE-Info fork (its `src/extras/` overlay must not be restored). Synthesis: `graphify-out/foe-info/findings/2026-09-12-quad-graph-executive-synthesis.md`.
- **Metadata-Store Sibling Relocation** — RESOLVED. Moved `metadata-store` to
  `../metadata-store`, aligning symmetrically with `FoE-Info-Extension-original`,
  `LoW-Tool`, and `forge-hammer`. Graph exports (`wiki/`, `obsidian/`, `svg`, `tree`)
  and download scripts updated and verified.
- **Forge-Hammer Agent Cleanup** — RESOLVED. Removed internal `.agents/`,
  `AGENTS.md`, `tests/`, and `package.json` from `../forge-hammer`, retaining
  strictly graphify-out and `.gitignore` to match peer baseline standards.
- **General Specialist Decoupling** — RESOLVED. Restored 11 technical subagent
  personas (`javascript-expert`, `typescript-expert`, `webpack-expert`, etc.)
  to general domain expertise without FoE-Info-specific hardcoding.
- **Cold-login delay** — RESOLVED. Was Debug Mode's own logging overhead, not a
  real bug. Standard Mode cold entry ~5.49s; middleware subscriber removes the
  spinner ~100ms before resolver finish (not confirmed visible-wrong-data).
- **Graphify local backend** — RESOLVED. Shared launcher wires env vars into
  `graphify-*` MCP servers + `graphify watch`.
- **Persona-loading** — RESOLVED. `.agents/agents/*.md` are connected via
  manual/instruction-driven subagent delegation, not auto-loaded.
- **Full ecosystem audit** — verified 36 subagents / 17 rules / 53 skills;
  fixed 2 stale counts. Gaps: rules not auto-injected by trigger mode in hosts;
  hooks enforced by host adapters.
- **Hook parity** — `graphify-sync`, `graphify-guard`, `monolith-guardrail`
  built + live-verified; `stop-guard` logs a warning plugin (no `fullyIdle`
  equivalent).
- **opencode takeover** — RESOLVED. `opencode.json` (17 rules as instructions,
  7 MCP servers), 36 shims in `.opencode/agents/`, 4 hook plugins live-verified.
  Codex/Claude compatibility layer decommissioned 2026-09-09; opencode is the
  sole coding host.

## Open threads requiring a decision

- ~~`backup-development-2026-09-06` worktree~~ — Resolved: verified deleted; git worktree list clean, no active worktrees under `.worktrees/`.
- `graphify-out/foe-info/obsidian/` + `wiki/` markdown exports — git-ignored
  generated artifacts from the graphify knowledge graphs; retained locally.
- ~~Town Hall / Beta Debug panel height & scroll behavior (`betaDebugPanel.js`).~~ Resolved 2026-09-11: Bootstrap-owned collapse, inline overrides removed, `beta:height` persistence via `bindResizableCollapse`.

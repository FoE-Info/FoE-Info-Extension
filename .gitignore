.cache/
node_modules/
build/
graphify-out/cost.json        # local only
graphify-out/cache/            # optional: commit for speed, skip to keep repo small
.vscode/settings.json

# Claude Code: personal/local-only, not shared project config
.claude/scheduled_tasks.lock
.claude/*.graphify-bak

# aas: local install state tied to this machine (not the declarative stack)
.aas/
aas-plan.json

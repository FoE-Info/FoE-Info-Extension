; typescript-eslint@8.x peers typescript <6.1.0, but the project intentionally
; runs typescript@7.x for the hybrid TS migration. Relax peer validation so the
; lint toolchain installs reproducibly until upstream supports TS >= 7.1.
legacy-peer-deps=true

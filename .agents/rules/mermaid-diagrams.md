# Mermaid Diagram Rules

- **gitGraph Commit IDs**: When creating Mermaid `gitGraph` diagrams, `commit id:` values MUST be concise alphanumeric strings or short commit hashes (e.g., `"5080590"`). Do NOT include spaces, colons, parentheses, or long commit descriptions inside the `id` field.
